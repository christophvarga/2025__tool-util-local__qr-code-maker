# apps--qr-code-maker .env.example
# Generated from docker-compose.yml (2026-05-22 — Wave-6 D7-Follow-Up)
#
# This compose file is fully self-contained: the qr-code-maker service
# does not reference any ${VAR} substitutions from .env. All runtime
# config (Traefik labels, network, healthcheck) is hard-coded.
#
# This file exists only to satisfy fleet-wide env-coverage scans
# (check-all-compose-env-coverage.sh). No keys are required.
#
# If you ever add a ${VAR} to docker-compose.yml or docker-compose.hetzner.yml,
# declare it here with an empty value and an inline comment describing usage.
