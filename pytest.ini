[pytest]
testpaths = 87_tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short

# Playwright settings
; Default browser (can be overridden with --browser)
; Headless mode by default
